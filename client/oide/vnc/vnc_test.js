'use strict';

describe('oide.vnc module', function() {

  beforeEach(module('oide.vnc'));

  describe('vnc controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var vncCtrl = $controller('VncCtrl');
      expect(vncCtrl).toBeDefined();
    }));

  });
});
