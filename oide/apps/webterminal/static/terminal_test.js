'use strict';

describe('oide.terminal module', function() {

  beforeEach(module('oide.terminal'));

  describe('terminal controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var terminalCtrl = $controller('TerminalCtrl');
      expect(terminalCtrl).toBeDefined();
    }));

  });
});